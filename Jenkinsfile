pipeline {
    agent any 

     environment {
        AWS_ACCESS_KEY_ID     = credentials('lambda-aws-secret-key-id')
        AWS_SECRET_ACCESS_KEY = credentials('lambda-aws-secret-access-key')
    }      
